var timeoutID = undefined;
var intervalID = undefined;

var inputReader = require("readline-sync");

var answer = inputReader.question("Interval Example(y/n)(ps enter 'n' to do Timeout Example)? ");
if(answer === 'y')
{
    
}
else
{
    
}

